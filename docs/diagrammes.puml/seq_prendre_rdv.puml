@startuml seq_prendre_rdv
!include diagram_style.iuml

autonumber
actor Éleveur as ELE
boundary "Interface web" as UI
control "ElevConnect" as APP
database "Base de données" as DB
actor Vétérinaire as VET

title Rendez-vous vétérinaire — vue simplifiée

ELE -> UI : choisit un service et un créneau
UI -> APP : demanderRendezVous()
APP -> DB : enregistre la demande
APP -> VET : notifie la demande

VET -> UI : confirme ou refuse
UI -> APP : traiterRendezVous(décision)
APP -> DB : met à jour le statut
APP -> ELE : notifie la décision

opt rendez-vous confirmé
  VET -> UI : clôture le rendez-vous
  UI -> APP : marquerRéalisé()
  APP -> DB : enregistre la réalisation
  ELE -> UI : laisse un avis
  UI -> APP : enregistrerAvis()
end

note over ELE, VET
  Le règlement se fait directement
  entre l'éleveur et le vétérinaire.
end note
@enduml
